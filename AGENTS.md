# AI Project Baseline — Codex Entry Point

This repository uses the shared AI manifest in `.claude/CLAUDE.md`.

Before starting any code task:

1. Read `.claude/CLAUDE.md`.
2. Follow its LOAD PROTOCOL.
3. Load/apply only skills whose `enabled` and `context` match the CONFIG.
4. If `project_type = PENDING` or the user requests setup, follow `.claude/agents/setup.md`.

Do not duplicate or override the manifest here. Treat `.claude/CLAUDE.md` as the source of truth.
